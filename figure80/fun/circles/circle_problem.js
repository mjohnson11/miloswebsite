
// ok so a circle, a bar graph with the prob of being alive at each step, 
// a bar graph with the increase in the circle size if alive at each step

function make_random_point(cx, cy, r) {
  let circle_point = 
}